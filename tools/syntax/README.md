
This directory contains definitions to enable syntax highlighting of
CVT and CNA files in VIM.

Copy `csirac-assembler.vim` and `csirac-tape.vim` to `$HOME/.vim/syntax`
and copy `csirac-detect.vim` to `$HOME/.vim/ftdetect`
